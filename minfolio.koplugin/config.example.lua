-- SPDX-License-Identifier: AGPL-3.0-only
-- Optional local Minfolio config.
-- Copy this file to config.lua on the Kindle plugin install if you want to override defaults.
-- The real config.lua is gitignored so device-specific settings do not get committed.

return {
    notes_dir = "/mnt/us/notes",
    state_dir = "/mnt/us/minfolio",
    minfolio_scale = 1.0,
}
